import express from 'express';
import { reviewControllerExports } from './../controllers/reviewController';

const router = express.Router();

export const userRoutesExports = {
    router: router,
};
